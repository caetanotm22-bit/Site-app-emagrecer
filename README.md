# Nexto — Landing Page

Site de vendas do app Nexto, construído em HTML/CSS/JS puro.

## Deploy no Vercel

Este repositório está pronto para deploy direto no Vercel.

- Framework: **Other** (HTML estático)
- Build Command: *(vazio)*
- Output Directory: *(vazio)*
- Root Directory: `.`

A raiz `/` redireciona automaticamente para `nexto.html` via `vercel.json`.

## Estrutura

```
/
├── nexto.html       ← Página principal
├── nexto.css        ← Estilos
├── nexto.js         ← Scripts
├── vercel.json      ← Config Vercel
└── img/             ← Imagens otimizadas
```
